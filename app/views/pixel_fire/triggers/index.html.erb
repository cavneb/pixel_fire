<p id="notice"><%= notice %></p>

<h1>Triggers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Fire on all pages</th>
      <th>Page url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @triggers.each do |trigger| %>
      <tr>
        <td><%= trigger.name %></td>
        <td><%= trigger.fire_on_all_pages %></td>
        <td><%= trigger.page_url %></td>
        <td><%= link_to 'Show', trigger %></td>
        <td><%= link_to 'Edit', edit_trigger_path(trigger) %></td>
        <td><%= link_to 'Destroy', trigger, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trigger', new_trigger_path %>
